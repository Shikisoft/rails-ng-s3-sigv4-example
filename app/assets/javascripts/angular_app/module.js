//Initialization and configuration of ppNgApp
// 
//This file declares and loads Angular module. It should be the first file to be loaded before controllers/services/etc...

//Initialize uploadApp
var app = angular.module('uploadApp', [ 
    'ngFileUpload'
]);
